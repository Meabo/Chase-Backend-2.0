export class GameParameters {
  static generateChaseObjectDelay: number = 3000; // in ms
  static distanceToCatch: number = 10000; // in meters
  static distanceToSteal: number = 10000; // in meters
  static scoreCatch: number = 1000; // in points
  static scoreSteal: number = 1000; // in points
}